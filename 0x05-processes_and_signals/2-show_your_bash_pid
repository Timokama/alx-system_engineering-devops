splays lines containing the bash word
# shellcheck disable=SC2009
# grep - Search specific word and execute
ps -eo user,pid,%cpu,%mem,vsz,rss,tname,stat,start_time,bsdtime,args --forest |\
 grep bash
